# STM32F4 Automotive Headlights & Turn Indicators Controller

Firmware prototype for an automotive lighting controller targeting the STM32F413ZH. The system manages headlight beams and turn indicators via CAN bus commands and GPIO switch inputs, with power monitoring over I2C.

## What It Does

- **Headlights** — PWM-controlled high-beam and low-beam outputs. Beam selection is commanded over CAN bus (IDs: `CAN_ID_HIGH`, `CAN_ID_LOW`, `CAN_ID_OFF`).
- **Turn indicators / hazard** — Left, right, and hazard (both) blink patterns driven by GPIO switch inputs, toggled at a fixed 100 ms cadence by a dedicated FreeRTOS task.
- **Power monitoring** — INA219 current/voltage sensor read over I2C in master mode by a periodic polling task; exposes the latest shunt voltage, bus voltage, and current.
- **Self-test on boot** — A compile-time flag (`RUN_BOOT_SELFTEST` in `main.h`) gates a headlights and indicators test sequence that runs before the RTOS scheduler starts.

## Firmware Architecture (FreeRTOS tasks)

The application runs three independent CMSIS-OS v1 tasks, each owning a single input/output path (no shared event bus — each task reads its own input and drives its own output):

| Task | Priority | Cadence | Responsibility |
|---|---|---|---|
| `StartCAN_Task` | Idle | ~1 ms poll | Reads CAN RX FIFO0 and sets the beam PWM (`TIM1` CCR1/CCR2) from `CAN_ID_HIGH` / `CAN_ID_LOW` / `CAN_ID_OFF`. Sole writer of the beam outputs. |
| `StartIndicators_Task` | Normal | 100 ms | Samples the left/right switch inputs and toggles the indicator LEDs (left, right, hazard). |
| `StartINA219_Task` | Low | 500 ms | Polls the INA219 over I2C (master mode) and stores the latest bus voltage, shunt voltage (signed µV), and current. |

## Hardware Architecture

```
                        ┌─────────────────────────────┐
                        │       STM32F413ZH            │
                        │                              │
  CAN Bus ─────────────►│ CAN1          TIM1 CH1/CH2 ──┼──► High / Low Beam (PWM)
                        │                              │
  Left switch ─────────►│ PF12 (GPIO)   PF15 (GPIO) ───┼──► Left indicator LED
  Right switch ─────────►│ PA7  (GPIO)   PE13 (GPIO) ───┼──► Right indicator LED
                        │                              │
  INA219 sensor ────────►│ I2C1 (100 kHz)              │
                        │                              │
  Duty cycle input ─────►│ TIM2 CH1/CH2  (PWM capture) │
  Duty cycle input ─────►│ TIM3 CH1/CH2  (PWM capture) │
                        └─────────────────────────────┘
```

### CAN Bus Test Setup (bench source of the commands)

This repository contains only the **receiving** ECU node — the STM32F413ZH above, which listens on CAN1 and drives the beam PWM outputs. During development, the CAN commands it received were generated on the bench by a separate sending node, not by this codebase:

```
Push Button ──► Arduino (GPIO input) ──► MCP2515 CAN Controller (SPI) ──► Onboard
                                                                        Transceiver
                                                                            │
                                                                  CAN_H / CAN_L (bus)
                                                                            │
                                                        STM32 CAN Transceiver (e.g. TJA1050)
                                                                            │
                                                                    STM32F413ZH CAN1 RX
```

- A push button wired to an Arduino GPIO pin triggered the message.
- The Arduino used an add-on **MCP2515 CAN Bus module** (MCP2515 controller + onboard transceiver, communicating with the Arduino over SPI) to construct and transmit standard 11-bit CAN frames — `CAN_ID_HIGH` (`0x102`), `CAN_ID_LOW` (`0x101`), or `CAN_ID_OFF` (`0x103`) — with a 1-byte payload, typically via the common `mcp_can` Arduino library.
- Those frames travelled over the physical CAN_H/CAN_L bus to the STM32 side, which has its own CAN transceiver converting the differential bus signal back into logic-level TX/RX for the STM32's built-in bxCAN peripheral.
- The transmit/sending logic (Arduino + MCP2515 firmware) is **not part of this repository**; this repo covers the STM32 receiving ECU only — peripheral configuration, RTOS task scheduling, ID-based dispatch, and PWM beam control.

## Technologies Used

| Layer | Details |
|---|---|
| MCU | STM32F413ZH @ 70 MHz (HSE + PLL) |
| RTOS | FreeRTOS via CMSIS-OS v1 — three tasks: CAN, Indicators, INA219 |
| CAN bus | CAN1, normal mode, accept-all RX filter on FIFO 0 |
| I2C | I2C1 @ 100 kHz, master mode (reads the INA219) |
| PWM output | TIM1 CH1 & CH2, 1 kHz, 0–100% duty cycle |
| PWM input capture | TIM2 & TIM3, input capture mode |
| Power monitor | INA219 (Texas Instruments) — 32 V / 2 A calibration |
| HAL | STM32 HAL + LL, generated by STM32CubeMX |
| Bench CAN source | Arduino + MCP2515 CAN module (external to this repo) |

## Project Structure

```
Core/
  Src/
    main.c           — entry point, peripheral init, FreeRTOS task definitions
    headlights.c     — headlight state machine (high/low/off)
    indicators.c     — turn indicator blink logic
    ina219.c         — INA219 driver (read/write registers, calibration)
  Inc/
    headlights.h
    indicators.h
    ina219.h
Drivers/            — STM32 HAL & CMSIS (generated)
Middlewares/        — FreeRTOS (generated)
```

## Building

This is an [STM32CubeIDE](https://www.st.com/en/development-tools/stm32cubeide.html) project targeting the **STM32F413ZH** (e.g. the NUCLEO-F413ZH board).

1. **Clone** the repository.
2. **Import** into STM32CubeIDE: *File → Import → Existing Projects into Workspace*, and select the repository root.
3. **Build**: *Project → Build All* (`Ctrl+B`). The `arm-none-eabi-gcc` toolchain ships with CubeIDE.
4. **Flash / debug**: connect the board via ST-LINK and *Run → Debug* to program and start a debug session.

The peripheral configuration can be inspected or regenerated from `HeadlightsPrototype.ioc` with STM32CubeMX. To skip the power-on lamp self-test, set `RUN_BOOT_SELFTEST` to `0` in `Core/Inc/main.h`.

## License

Released under the [MIT License](LICENSE). Bundled third-party code — STMicroelectronics HAL/CMSIS (`Drivers/`) and FreeRTOS (`Middlewares/`) — retains its own license terms.

## Known Limitations

- This repo contains the receiving ECU node only. The CAN commands (`CAN_ID_HIGH` / `CAN_ID_LOW` / `CAN_ID_OFF`) were generated on the bench by a separate Arduino + MCP2515 sending node (see "CAN Bus Test Setup" above); that transmit-side firmware is not included here.
- The INA219 is read in master mode (`ina219.c`) by a periodic polling task that stores the latest bus voltage, shunt voltage, and current in a global. Those values are not yet consumed downstream (e.g. reported over CAN or displayed).
- A single accept-all CAN filter (ID=0, mask=0) is configured; all incoming IDs reach FIFO0 and are dispatched in software by `StdId`.
- **PA0/PA6 are double-configured.** Both pins are set up as TIM2/TIM3 PWM input-capture channels (see `.ioc`) and are *also* read as digital GPIO in `headlights.c`. The intended role of these pins (beam-emission feedback vs. control input) isn't settled, so the conflicting GPIO read is left in place and flagged here rather than removed.
- Stack sizes for FreeRTOS tasks (128 words each) are minimal and untested under load.
